/* Import fonts */
@import url('https://fonts.googleapis.com/css2?family=Source+Sans+Pro:wght@400;600;700&display=swap');

/* Variables */
:root {
  --primary: 0 60% 69%;
  --primary-foreground: 0 0% 100%;
  --primary-hover: 0 60% 59%;
  --hero-bg: 220 15% 15%;
  --hero-overlay: 220 15% 10%;
  --hero-text: 0 0% 100%;
  --section-bg: 0 0% 100%;
  --section-text: 220 15% 25%;
  --section-muted: 220 10% 50%;
  --section-border: 220 15% 90%;
  --pentagon-primary: 0 60% 69%;
  --pentagon-bg: 0 0% 95%;
  --input-bg: 0 0% 98%;
  --input-border: 220 15% 85%;
  --input-focus: 0 60% 69%;
  --background: 0 0% 100%;
  --foreground: 220 15% 25%;
  --card: 0 0% 100%;
  --card-foreground: 220 15% 25%;
  --border: 220 15% 85%;
  --input: 0 0% 98%;
  --ring: 0 60% 69%;
  --radius: 0.5rem;
}

/* Base Reset */
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
  border-color: hsl(var(--border));
}

body {
  background: hsl(var(--background));
  color: hsl(var(--foreground));
  font-family: 'Source Sans Pro', sans-serif;
  line-height: 1.5;
}

img {
  max-width: 100%;
  display: block;
}

/* Hero section */
.hero-section {
  position: relative;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, hsl(var(--hero-bg)), hsl(var(--hero-overlay)));
  color: hsl(var(--hero-text));
  text-align: center;
  padding: 20px;
}

.hero-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(135deg, rgba(0,0,0,0.3), rgba(0,0,0,0.7));
}

.hero-section h1 {
  font-size: 3rem;
  z-index: 1;
}

.hero-section p {
  font-size: 1.2rem;
  margin-top: 10px;
  z-index: 1;
}

.btn-primary {
  background: hsl(var(--primary));
  color: hsl(var(--primary-foreground));
  padding: 12px 32px;
  border-radius: 999px;
  font-weight: 600;
  transition: all 0.3s ease;
  z-index: 1;
}

.btn-primary:hover {
  opacity: 0.9;
  transform: scale(1.05);
}

/* Nav */
nav {
  display: flex;
  gap: 20px;
  z-index: 1000;
}

.nav-link {
  color: hsl(var(--hero-text));
  transition: color 0.3s;
}

.nav-link:hover,
.nav-link:active {
  color: hsl(var(--primary));
}

/* Section containers */
.section-container {
  max-width: 1000px;
  margin: 0 auto;
  padding: 40px 20px;
}

.section-title {
  font-size: 2rem;
  font-weight: bold;
  text-align: center;
  margin-bottom: 16px;
  color: hsl(var(--section-text));
}

.section-subtitle {
  font-size: 1.1rem;
  text-align: center;
  margin-bottom: 30px;
  max-width: 600px;
  margin-left: auto;
  margin-right: auto;
  color: hsl(var(--section-muted));
}

/* Pentagon */
.pentagon {
  position: relative;
  width: 190px;
  height: 160px;
  margin: 0 auto;
}

.pentagon::before {
  content: '';
  position: absolute;
  top: 0;
  left: 50%;
  transform: translateX(-50%);
  border-left: 95px solid transparent;
  border-right: 95px solid transparent;
  border-bottom: 69px solid hsl(var(--pentagon-primary));
}

.pentagon::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 180px;
  height: 120px;
  background: hsl(var(--pentagon-primary));
  clip-path: polygon(0% 0%, 100% 0%, 82% 100%, 18% 100%);
}

/* Form styles */
.form-input {
  width: 100%;
  padding: 12px;
  border-radius: 8px;
  border: 1px solid hsl(var(--input-border));
  background: hsl(var(--input-bg));
  transition: 0.3s;
}

.form-input:focus {
  outline: none;
  border-color: hsl(var(--input-focus));
  box-shadow: 0 0 0 3px hsla(var(--input-focus), 0.1);
}

/* Footer */
footer {
  background: hsl(var(--hero-bg));
  color: white;
  text-align: center;
  padding: 20px;
}

footer .socials a {
  color: white;
  margin: 0 8px;
  text-decoration: none;
}

footer .socials a:hover {
  color: hsl(var(--primary));
}

/* Hover animations */
.feature-card:hover {
  transform: scale(1.05);
  box-shadow: 0px 4px 20px rgba(0,0,0,0.1);
}

.result-card:hover {
  transform: scale(1.1) rotate(2deg);
}

/* Hamburger Styles */
.hamburger {
  display: none;
  flex-direction: column;
  cursor: pointer;
  gap: 5px;
  z-index: 1001;
}

.hamburger span {
  display: block;
  width: 25px;
  height: 3px;
  background: white;
  transition: 0.3s;
}

.hamburger.active span:nth-child(1) {
  transform: rotate(45deg) translate(5px, 5px);
}
.hamburger.active span:nth-child(2) {
  opacity: 0;
}
.hamburger.active span:nth-child(3) {
  transform: rotate(-45deg) translate(5px, -5px);
}

/* Mobile Navigation */
@media (max-width: 480px) {
  .hamburger {
    display: flex;
    position: absolute;
    top: 20px;
    right: 20px;
  }

  nav {
    position: absolute;
    top: 60px;
    right: 0;
    background: rgba(0, 0, 0, 0.95);
    flex-direction: column;
    padding: 20px;
    width: 200px;
    display: none;
    gap: 15px;
    z-index: 1000;
  }

  nav a {
    color: white;
    text-decoration: none;
  }

  nav.open {
    display: flex;
    animation: slideDown 0.3s ease-in-out forwards;
  }

  @keyframes slideDown {
    from {
      opacity: 0;
      transform: translateY(-10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
}
